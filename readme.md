# =========Banking widget==========


## Banking widget - это backend, который готовит данные для отображения в widget, в личном кабинета клиента крупного банка. Этот виджет показывает несколько последних успешных банковских операций клиента.

## -------------Установка и использование-------------

### **Требования (Requirements):**

1. **Python** версии 3.14.3
2. **Poetry** версия 2.3.2
3. Смотрите файл `pyproject.toml` для получения информации о версиях зависимостях проекта.

### **Установка:**

1. **Клонируйте проект** с этого репозитория, используя в PyCharm (в командной строке терминала) команду:
```
git clone https://github.com/Dmitrii-dev-coder/backend_banking_widget.git
```
2. **Инициализируйте виртуальное окружение** командой:
```
python -m venv <название_окружения>
```
*Например:*
```
python -m venv venv
```
Однако важно убедиться, что у вас корректно настроен путь к Python в системной переменной `PATH`, чтобы команда работала без ошибок. О том, как добавить Python в PATH в Windows 11, смотри [здесь](https://ip-calculator.ru/blog/windows-11/how-to-add-python-to-path-on-windows-11-2/).
3. **Активируйте окружение** с помощью команды:
*   на macOS и Linux:
```
source venv/bin/activate
```
*   на Windows:
```
venv\Scripts\activate
```
*Или используйте `poetry shell` для активации виртуального окружения (доступно только при установленном плагине `poetry-plugin-shell`).*
4. **Установите все зависимости проекта** (указанные в файле `requirements.txt`) командой:
```
pip install -r requirements.txt
```
Также можно установить линтеры на выбор, используя Poetry с помощью команды:
```
poetry add --group lint <линтер>
```
Замените <линтер> на имя нужного линтера, например `flake8` или `mypy`, и они будут указаны в pyproject.toml. 
Если же использовать команду:
```
poetry add <линтер>
```
Тогда линтеры будут установлены для всего PyCharm


### **Примеры использования:**

1. **Точка входа:** В корне рабочей директории проекта откройте модуль `main.py`. Все функции в модулях папки `src` 
проверяются и используются отсюда.


2. **Проверка функции `get_mask_card_number` из модуля `masks.py`:**
В файле `main.py` найдите функцию `checking_masks_functions` (начинается с 8-й строки). Для проверки в коде этой 
функции уже подставлен номер карты в строке 11. Либо вы можете заменить этот номер карты на свой вариант.
Запустите модуль командой:
```
python main.py
```
Среди результатов первая строка будет результатом работы функции:
> Замаскированный номер карты: 7000 79** **** 6361

3. **Проверка функции `get_mask_account` из модуля `masks.py`:**
В файле `main.py` найдите ту же функцию `checking_masks_functions`. Для проверки этой функции в строке 12 уже 
подставлен номер счета. Либо вы можете заменить его на свой.
Запустите модуль той же командой:
```
python main.py
```
Среди результатов вторая строка будет результатом работы функции:
> Замаскированный номер счета: **4305

4. **Проверка функции `mask_account_card` из модуля `widget.py`:**
В файле `main.py` найдите функцию `checking_widget_functions1` (начинается с 22-й строки). Для проверки в строке 57 
уже подставлен номер счета. Либо вы можете заменить его на свой вариант (примеры вариантов смотри ниже в строках 62-67).
Запустите модуль командой:
```
python main.py
```
Среди результатов третья строка будет результатом работы функции:
> Счет **9589

5. **Проверка функции `get_date` из модуля `widget.py`:**
В файле `main.py` найдите функцию `checking_widget_functions2` (начинается с 31-й строки). Для проверки в строке 58 
уже подставлена дата. Либо вы можете заменить её на свой вариант.
Запустите модуль командой:
```
python main.py
```
Среди результатов четвертая строка будет результатом работы функции:
> 11.03.2024

6. **Проверка функции `filter_by_state` из модуля `processing.py`:**
В файле `main.py` найдите функцию `checking_filter_by_state` (начинается с 40-й строки). Для проверки в строках с 77-й 
по 82-ю уже подставлены данные. Либо вы можете заменить их на свои. Второй аргумент не обязательный (по умолчанию 
используется "EXECUTED"), но вы можете добавить "CANCELED".
Запустите модуль командой:
```
python main.py
```
Среди результатов отобразится отфильтрованный список в пятой строке:
> [{'id': 594226727, 'state': 'CANCELED', 'date': '2018-09-12T21:27:25.241689'}, {'id': 615064591, 'state': 'CANCELED', 'date': '2018-10-14T08:21:33.419441'}]

7. **Проверка функции `sort_by_date` из модуля `processing.py`:**
В файле `main.py` найдите функцию `checking_sort_by_date` (начинается с 47-й строки). Для проверки в строках с 89-й по 94-ю уже подставлены данные для сортировки.
```
python main.py
```
Среди результатов отобразится отсортированный список в шестой строке:
> [{'id': 939719570, 'state': 'EXECUTED', 'date': '2018-06-30T02:08:58.425572'}, {'id': 594226727, 'state': 'CANCELED', 'date': '2018-09-12T21:27:25.241689'}, {'id': 615064591, 'state': 'CANCELED', 'date': '2018-10-14T08:21:33.419441'}, {'id': 41428829, 'state': 'EXECUTED', 'date': '2019-07-03T18:35:29.512364'}]

## -------------------------Лицензия--------------------------

[MIT](https://ru.wikipedia.org/wiki/%D0%9B%D0%B8%D1%86%D0%B5%D0%BD%D0%B7%D0%B8%D1%8F_MIT?ysclid=moiibt5dy6413846171)

## -------------------------Контакты--------------------------

- Мой email: `dharani@mail.ru`
- Писать и звонить через Макс: `+7 904 968 64 63`


